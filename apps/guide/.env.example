# Local dev: point at the Worker running via `wrangler dev`.
# Production: https://api.thetalusfieldjournal.com (committed in
# .env.production; a Cloudflare Pages env var of the same name overrides).
VITE_API_BASE=http://localhost:8787
